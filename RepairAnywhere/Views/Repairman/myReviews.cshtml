@model RepairAnywhere.Models.RepairmanViewModel.MyReviewViewModel
<div class="row">
            <div class="col-lg-12">
                    <h2 class="page-header">MyReviews</h2>
                </div>
                <!-- /.col-lg-12 -->
            </div>
<table class="table table-primary table-striped">
                            
                             
    
                            @{int c = 0;}
                            @foreach (var item in @Model.reviews)
                            {
                                
                                <tr >
								<td width="10%"><img src="~/Styles/Customer/images/user_pic.jpg" style="width:100%" class="rounded-circle"> <b>@Model.customers[c].Name</b></td>
								<td class="text-justify">@item.Comment</td>
                                    <td></td>
							</tr>
                           
                                c++;
                            }
							
						</table>